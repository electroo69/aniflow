/**
 * Application Configuration
 * Update BASE_URL to your production domain
 */

export const config = {
    // Update this to your actual production domain
    BASE_URL: 'https://aniflow.gyatbox.space',

    // Site metadata
    siteName: 'AniFlow',
    siteDescription: 'Discover your next favorite anime and manga on AniFlow. Browse top charts, seasonal releases, and find your next obsession.',

    // Default Open Graph image
    defaultOgImage: '/og-image.jpg',
};
